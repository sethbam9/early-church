export function KeyboardShortcutHelp() {
  return (
    <div className="shortcut-help">
      <strong>Keyboard:</strong> Space play/pause, ←/→ decade, / focus search, L toggle controls,
      D toggle details, Esc clear selection, ? help.
    </div>
  );
}
